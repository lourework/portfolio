---
import { url } from '../utils/url'
const pathname = Astro.url.pathname
const navLinks = [
  { href: url(''), label: 'home' },
  { href: url('work'), label: 'work' },
  { href: url('about'), label: 'about' },
]
---
<style>
  @keyframes blink {
    0%, 88%, 100% { transform: scaleY(1); }
    92%           { transform: scaleY(0.12); }
    96%           { transform: scaleY(1); }
  }
  .blink-face {
    display: inline-block;
    animation: blink 3.5s infinite;
    transform-origin: center 60%;
  }
</style>
<nav class="fixed top-0 left-0 right-0 z-50 bg-[var(--color-bg)]/90 backdrop-blur-sm border-b border-[var(--color-border)]">
  <div class="max-w-5xl mx-auto px-6 h-16 flex items-center justify-between">
    <a href={url('')} class="font-serif text-lg font-semibold tracking-tight">
      <span class="text-[var(--color-accent)]">Babi</span>.
    </a>
    <ul class="flex gap-8 items-center">
      {navLinks.map(({ href, label }) => (
        <li>
          <a
            href={href}
            class={`text-sm tracking-wide transition-colors ${
              pathname === href || (href !== '/' && pathname.startsWith(href))
                ? 'text-[var(--color-text)] font-medium'
                : 'text-[var(--color-muted)] hover:text-[var(--color-accent)]'
            }`}
          >
            {label}
          </a>
        </li>
      ))}
      <li>
        <a
          href="https://drive.google.com/file/d/1ulmNJgsgWb_F0uNBbFBwGC8nOPQ_4ghf/view?usp=sharing"
          target="_blank"
          rel="noopener"
          class="text-sm tracking-wide px-3 py-1.5 border border-[var(--color-text)] text-[var(--color-text)] hover:bg-[var(--color-text)] hover:text-white transition-colors"
        >
          resume ↗
        </a>
      </li>
    </ul>
  </div>
</nav>
<div class="h-16" />
